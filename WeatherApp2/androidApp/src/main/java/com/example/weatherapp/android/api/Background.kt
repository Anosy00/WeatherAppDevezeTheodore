package com.example.weatherapp.android.api

import org.json.JSONObject

class Background {
    fun setBackgroundImage(json: JSONObject){
        when (json.get("icon")){
            snow ->
        }
    }
}